name: Generate Snake Animation

on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Generate contribution snake SVGs
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            assets/snake/github-contribution-grid-snake.svg?color_snake=#A67C2D&color_dots=#ebedf0,#9bb4d0,#5b84b0,#1E4A7A,#123258
            assets/snake/github-contribution-grid-snake-dark.svg?color_snake=#C9A227&color_dots=#0d1117,#1a2740,#24486e,#3d6fa3,#5b8fc7


      # Committing to main lets the README load the snake as a repository-relative
      # asset, which GitHub serves same-origin like every other image on the profile.
      - name: Commit refreshed snake
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add assets/snake
          if git diff --staged --quiet; then
            echo "Snake unchanged"
          else
            git commit -m "chore: refresh contribution snake"
            git push
          fi
